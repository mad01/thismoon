# build output (root-anchored: webkit/dist/ is intentionally committed)
/bin/
/dist/

# editor / OS noise
.DS_Store
*.swp

# local env
.envrc
.env

# generated in CI by the changes job, never committed
.github/filters.generated.yml
