# Template for ~/.config/myservice/secrets.env — copy to secrets.env on each
# machine and fill in. secrets.env is gitignored; this template is committed so
# the required keys are discoverable. Keys only, never real values.
MYSERVICE_API_TOKEN=
MYSERVICE_WEBHOOK_URL=
