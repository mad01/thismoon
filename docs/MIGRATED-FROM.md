# Migrated from

Every directory imported from another repo gets a row here at import time. Imports are clean (no git history — see docs/adr/0001), so this table is the only link back to where the code came from.

| Directory | Source repo | SHA at import | Imported | Notes |
|-----------|-------------|---------------|----------|-------|
| `webkit/` | github.com/mad01/webkit | `5a18e5d02f129f828733dbc2214151827dccc85c` | 2026-07-05 | Dropped `go.mod` and `.golangci.yml`; module path rewritten to `github.com/mad01/thismoon/webkit`; consumption docs updated for in-module use. |
| `services/reminder/` | github.com/mad01/dotfiles (`reminder/`) | `0a4582b7b6e3aace96e2d74813a8bdbe0a7fd66b` | 2026-07-05 | Dropped `go.mod`/`go.sum`; imports rewritten to `github.com/mad01/thismoon/services/reminder` and in-module webkit; removed `update-webkit` target and the webkit-pin field from `GET /version`. |
| `services/status/` | github.com/mad01/dotfiles (`status/`) | `5b0d67562ef0e6aff6b4a97b18719fdbf78b4397` | 2026-07-05 | Dropped `go.mod`/`go.sum`; imports rewritten to `github.com/mad01/thismoon/services/status` and in-module webkit; removed `update-webkit` target and the webkit-pin field from `GET /version`. |
| `services/events/` | github.com/mad01/dotfiles (`events/`) | `5b0d67562ef0e6aff6b4a97b18719fdbf78b4397` | 2026-07-05 | Dropped `go.mod`/`go.sum`; imports rewritten to `github.com/mad01/thismoon/services/events` and in-module webkit; removed `update-webkit` target and the webkit-pin field from `GET /version`. |
| `services/deps/` | github.com/mad01/dotfiles (`deps/`) | `5b0d67562ef0e6aff6b4a97b18719fdbf78b4397` | 2026-07-05 | Same playbook edits as the other services; discovery `config.toml` ships beside the recipe (glob example in a code comment generalized). |
| `services/present/` | github.com/mad01/dotfiles (`present/`) | `5b0d67562ef0e6aff6b4a97b18719fdbf78b4397` | 2026-07-05 | Same playbook edits as the other services. MCP seatbelt wrapper + profile and the present skill moved to `recipes/present/`; webkit docs rewritten for in-module consumption (asset-hash `/webkit/version`). |
| `tools/t-man/` | github.com/mad01/t-man (whole repo) | `3d80ada2769b70fb1c9dc30608e8f965289b2ac2` | 2026-07-05 | First external-repo import (`git archive HEAD` of the full repo). Dropped `go.mod`/`go.sum`/`LICENSE` (root LICENSE covers it)/`.golangci.yml`/`mixer-system.yaml`; imports rewritten to `github.com/mad01/thismoon/tools/t-man`; Makefile rewritten to the fleet template (`~/code/bin`, "mad01 Local Signing" resign); `service-info.yaml` collapsed from own System to a Component under `system: thismoon`. |
| `services/d-man/` | github.com/mad01/dotfiles (`d-man/`) | `5d8cbd98fecfe5bb2194b56c7b95390307ae9452` | 2026-07-05 | Dropped `go.mod`/`go.sum`; imports rewritten to `github.com/mad01/thismoon/services/d-man` (not a webkit consumer, no webkit rewrite); Makefile was already the fleet template, only the LDFLAGS module path changed; one `os.Remove` errcheck fix for this repo's stricter lint config; SETUP.md doc paths repointed to `recipes/d-man/`. `routes.toml` + its symlink item stay in the dotfiles overlay (machine-personal route set). |
| `services/speak/` | github.com/mad01/dotfiles (`speak/`) | `a72ffc29b4c45fbf44c37d126e0b98be97877b18` | 2026-07-06 | Same playbook edits as the other services (webkit consumer: in-module imports, `update-webkit` target and `/version` webkit field removed). Go service only — the Kokoro TTS engine stays a recipe-managed sidecar (dotfiles `speak-tts` recipe); README documents that release artifacts require it. `sandbox-watch.sh` + `sandbox-audit.sh` + `sandbox-ignore.conf` moved to `recipes/speak/` (fleet-wide notifier, same pattern as present's seatbelt wrapper). |
| `services/csl/` | github.com/mad01/code-search-local (whole repo) | `ad031605f5a9b9d4074179b0e488af92274f60e7` | 2026-07-06 | Whole-repo import (`git archive HEAD`). Dropped `go.mod`/`go.sum`/`LICENSE`/`.golangci.yml`; imports rewritten to `github.com/mad01/thismoon/services/csl` and in-module webkit; removed `update-webkit` target and the webkit-pin field from `GET /version`; csl's dep pins seeded into the root `go.mod` before tidy (zoekt, hugot, grpc kept at tested versions); daemon protobufs regenerated for the descriptor's new go_package; `service-info.yaml` collapsed from own System to a Component under `system: thismoon`; example/fixture repo names in help text, docs, and tests renamed to `thismoon`; twelve `os.Remove` errcheck fixes for this repo's stricter lint config; test timeout raised to 120s (daemon suite flaked at 30s in the standalone repo too). Excluded from the release artifact matrix (unconditional cgo via tree-sitter; fleet installs from source). `config.yaml` symlink stays in the dotfiles overlay. |
| `services/catalog/` | github.com/mad01/catalog (whole repo) | `447bd2d7a898a6cff9aea415004958635064acfd` | 2026-07-06 | Whole-repo import (`git archive HEAD`). Dropped `go.mod`/`go.sum`/`LICENSE`/`.golangci.yml`/`.github`; imports rewritten to `github.com/mad01/thismoon/services/catalog` and in-module webkit; removed `update-webkit` target and the webkit-pin field from `GET /version`; `service-info.yaml` collapsed from own System + `catalog-cli` Component to a single Component `catalog` under `system: thismoon`; sample `registry.yaml` rewritten — the thismoon root row replaces the standalone `code-search-local`/`catalog`/`t-man` rows; remote-URL test fixtures renamed to `thismoon`. `registry.yaml` symlink stays in the dotfiles overlay. |
| `tools/belt/` | github.com/mad01/dotfiles (`belt/`) | `7dd0a991cc24388e15f2c9fa0c89e35c866bbcb6` | 2026-07-07 | Dropped `go.mod`/`go.sum`; imports rewritten to `github.com/mad01/thismoon/tools/belt`; internal-host doc wording generalized; config-overlay references repointed at the consuming repo (docs/adr/0006); BurntSushi/toml pin moved v1.5.0 -> v1.6.0 via the module fold. The `~/.config/belt/config.toml` symlink and the PreToolUse hook registration stay in the dotfiles overlay. |
| `tools/humanizer/` | github.com/mad01/dotfiles (`humanizer/`) | `7dd0a991cc24388e15f2c9fa0c89e35c866bbcb6` | 2026-07-07 | Dropped `go.mod`/`go.sum`; imports rewritten to `github.com/mad01/thismoon/tools/humanizer`; wiring docs rewritten for the two-layer split (docs/adr/0006); one `os.Remove` errcheck fix for this repo's stricter lint config; new root `.suspenders.yaml` paths ignore for the vale style pack's rule regexes. MCP registration, seatbelt wrapper + profile, and the `config.yaml` symlink stay in the dotfiles overlay. |
| `tools/worklog/` | github.com/mad01/dotfiles (`worklog/`) | `7dd0a991cc24388e15f2c9fa0c89e35c866bbcb6` | 2026-07-07 | Dropped `go.mod`/`go.sum`; imports rewritten to `github.com/mad01/thismoon/tools/worklog`; ticket-firewall strings (Linear prefixes, personal/internal path markers) made configurable via new `internal/config` + `~/.config/worklog/config.yaml` — hard-coded internal hostnames replaced by a derived non-github.com host check; internal ticket-key examples neutralized in docs, help text, and test fixtures. MCP registration, the config overlay, and the worklog skills stay in the dotfiles overlay. |
| `tools/suspenders/` | github.com/mad01/suspenders (whole repo) | `03908aef305a2caedc43de8b76668f8b5a3c5e66` | 2026-07-07 | Whole-repo import (`git archive HEAD`). Dropped `go.mod`/`go.sum`/`LICENSE`/`.golangci.yml`/`.goreleaser.yml`/`.github`/`.gitignore`/`CONTRIBUTING.md`; imports rewritten to `github.com/mad01/thismoon/tools/suspenders`; Makefile rewritten to the fleet template (kept `test-integration`, Docker-based); `service-info.yaml` collapsed from own System + `suspenders-cli` Component to a single Component `suspenders` under `system: thismoon`; the repo's own `.suspenders.yaml` fixture-path ignores folded into the root `.suspenders.yaml` scoped to `tools/suspenders/`; README install section rewritten for the monorepo and the guard's blocked-name collection documented in depth. The active guard config (`~/.config/suspenders/config.yaml` pattern set, blocked words, safe references) stays in the dotfiles overlay. |
| `tools/toss-bin/` | github.com/mad01/dotfiles (`toss-bin/`) | `9a49cb62ba5fd2650c2f9d15772f0a2355dd50ee` | 2026-07-19 | Swift package import (`git archive HEAD:toss-bin`) — the monorepo's first Swift component, outside the Go module. Replaced the `build` shell script with the fleet Makefile template (`swift build`/`swift test`, install kept at `~/.local/bin/toss-bin` with codesign resign); `VERSION` in `main.swift` annotated for release-please's generic updater (`extra-files`); ci.yml grew a Swift matrix (macos-15, Makefile-driven) and release.yml a `swift build` artifact branch; `service-info.yaml` moved under `system: thismoon`; docs rewritten to the shared outline. The `rm` shell function moves into `recipes/toss-bin` at cutover (was dotfiles `shell-aliases`). |
| `tools/humanizer/internal/scrub/`, `tools/humanizer/internal/rewrite/` | github.com/guillaumemeyer/watermarks-remover (`skills/remove-ai-marks/scripts/{text_unicode,rewrite_text}.py`) | `28eca2d91fd485213045b86896db671937432a48` | 2026-08-14 | Not a directory import — a Go reimplementation (port) of the text half of watermarks-remover, MIT-licensed. `internal/scrub` ports `text_unicode.py`'s Layer A deterministic Unicode/homoglyph inspect+clean; `internal/rewrite` ports `rewrite_text.py`'s Layer B prompt builder, bigram-divergence candidate selection, and loopback-default/no-redirect/env-key HTTP backends. Behaviour and test cases carried over from the Python tests; exposed as new `lint`/`fix`/`rewrite` CLI commands and `humanizer_lint`/`humanizer_fix`/`humanizer_rewrite` MCP tools. Network rewrite backends are CLI-only under the MCP seatbelt. |
