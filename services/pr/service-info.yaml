apiVersion: catalog.mad01/v1alpha1
kind: Component
metadata:
  name: pr
  description: Local PR review dashboard aggregating open PRs from multiple GitHub hosts
  tags: [go, cli, dashboard]
spec:
  type: cli
  lifecycle: development
  owner: mad01
  system: thismoon
