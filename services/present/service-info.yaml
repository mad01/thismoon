apiVersion: catalog.mad01/v1alpha1
kind: Component
metadata:
  name: present
  description: Serves single-page HTML briefings over localhost (CLI + MCP)
  tags: [go, cli, mcp]
spec:
  type: cli
  lifecycle: production
  owner: mad01
  system: thismoon
