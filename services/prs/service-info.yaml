apiVersion: catalog.mad01/v1alpha1
kind: Component
metadata:
  name: prs
  description: Open-PR dashboard — polls the GitHub hosts of every locally checked-out repo, CLI + MCP + webkit web page
  tags: [go, cli, mcp]
spec:
  type: cli
  lifecycle: development
  owner: mad01
  system: thismoon
