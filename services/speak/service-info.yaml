apiVersion: catalog.mad01/v1alpha1
kind: Component
metadata:
  name: speak
  description: Read markdown aloud over localhost, upload page + CORS proxy in front of local Kokoro TTS
  tags: [go, cli, tts]
spec:
  type: cli
  lifecycle: production
  owner: mad01
  system: thismoon
