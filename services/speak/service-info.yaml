apiVersion: catalog.mad01/v1alpha1
kind: Component
metadata:
  name: speak
  description: Text-to-speech over localhost, document audio API + CORS speech proxy for local pages, MCP tools for agents
  tags: [go, cli, tts]
spec:
  type: cli
  lifecycle: production
  owner: mad01
  system: thismoon
