apiVersion: catalog.mad01/v1alpha1
kind: Component
metadata:
  name: keep
  description: Local assertion store — evidence-pinned world model, CLI + MCP + webkit web page
  tags: [go, cli, mcp]
spec:
  type: cli
  lifecycle: development
  owner: mad01
  system: thismoon
