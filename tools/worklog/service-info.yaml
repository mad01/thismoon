apiVersion: catalog.mad01/v1alpha1
kind: Component
metadata:
  name: worklog
  description: Resumable, ticket/topic-keyed cross-session work state (CLI + MCP)
  tags: [go, cli, mcp]
spec:
  type: cli
  lifecycle: production
  owner: mad01
  system: thismoon
