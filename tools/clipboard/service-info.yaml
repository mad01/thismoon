apiVersion: catalog.mad01/v1alpha1
kind: Component
metadata:
  name: clipboard
  description: macOS system clipboard bridge over pbcopy/pbpaste (CLI + MCP)
  tags: [go, cli, mcp]
spec:
  type: cli
  lifecycle: production
  owner: mad01
  system: thismoon
