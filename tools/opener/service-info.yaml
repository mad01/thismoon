apiVersion: catalog.mad01/v1alpha1
kind: Component
metadata:
  name: opener
  description: macOS open bridge for URLs + files + apps + Finder reveal (CLI + MCP)
  tags: [go, cli, mcp]
spec:
  type: cli
  lifecycle: production
  owner: mad01
  system: thismoon
